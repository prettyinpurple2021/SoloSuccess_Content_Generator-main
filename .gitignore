# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files
.env
.env.local
.env.*.local
.env.production
.env.example
.env.local.example
.env.production.example
# Vercel configuration (if it contains secrets, uncomment the line below)
# vercel.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

*storybook.log
storybook-static

# API credentials and setup files
API_CREDENTIALS_SETUP.md

# Render deployment files (not used - deploying to Vercel only)
render.yaml
render.yml
.render/

# Docker files (not used - deploying to Vercel only)
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.yaml
nginx.conf
.docker/
