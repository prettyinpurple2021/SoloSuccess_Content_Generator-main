import React from 'react';
import App from '../../App';

// This is a wrapper around the main App component for authenticated users
export const DashboardPage: React.FC = () => {
  return <App />;
};

export default DashboardPage;
